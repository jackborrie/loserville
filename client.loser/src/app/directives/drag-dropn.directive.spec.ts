import {DragDropnDirective} from './drag-dropn.directive';

describe('DragDropnDirective', () => {
    it('should create an instance', () => {
        const directive = new DragDropnDirective();
        expect(directive).toBeTruthy();
    });
});
